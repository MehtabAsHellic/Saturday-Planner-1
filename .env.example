# Optional: Geocode Earth API key for city location lookup
# Get a free key at https://geocode.earth/
# Without this key, geocoding is skipped and the plan still works.
GEOCODE_EARTH_API_KEY=your_api_key_here
